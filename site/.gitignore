# Generated by `npm run types` (wrangler types) from wrangler.jsonc's
# bindings — regenerate rather than hand-edit, and don't commit it since it
# drifts from wrangler.jsonc/the real D1 database id.
worker-configuration.d.ts
